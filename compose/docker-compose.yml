services:
  db:
    container_name: base-mysql    
    image: mysql:8.0
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=123456
    ports:
      - 3308:3306
volumes:
  db_data: